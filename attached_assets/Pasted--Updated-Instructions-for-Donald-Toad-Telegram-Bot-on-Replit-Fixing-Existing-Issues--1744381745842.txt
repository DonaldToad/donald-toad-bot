# Updated Instructions for Donald Toad Telegram Bot on Replit

## 🔧 Fixing Existing Issues

### 🐸 1. Capital City Bug
**Problem**: When asked about a country, bot gives capital instead of political views.
**Fix**:
- Replace country lookup logic to return political opinion instead of geography:
```python
with open("donald_views.json") as f:
    political_opinions = json.load(f)

country = user_input.lower()
if country in political_opinions:
    response = political_opinions[country]
else:
    response = "I don't have strong croakpinions on that one yet! 🐸"
```

### 🎭 2. Jokes by Topic
**Problem**: Jokes always default to apes.
**Fix**:
- Expand the joke database into a JSON file `jokes_by_topic.json`:
```json
{
  "tariffs": ["Tariffs? Beautiful! Keeps foreign bugs OUT of our swamp. Croak on that! 🐸"],
  "defi": ["DeFi is like a frog party with no adult supervision. Wild. Profitable. Ribbit."],
  "dex": ["DEXs are like frog markets — no kings, just flips and swaps. Flies for everyone! 🐸"]
}
```
- Add logic to detect topic and pull jokes accordingly.

### 🤖 3. Nonsense Answer Bug
**Problem**: Bot responds to serious questions with nonsense.
**Fix**:
- Add keyword/topic detection before responding.
- Save recent user queries in `user_memory` and check for topic continuity.

```python
user_id = update.effective_user.id
memory = user_memory.get(user_id, [])
if 'defi' in memory[-1].lower() and 'which' in user_input:
    response = "For sure, Uniswap is big. Linea frogs love it. Even PancakeSwap's flipping around."
```

---

## 📝 Add List of Donald Toad Capabilities
Add a command `/features` that responds with:
```text
🐸 What can Donald Toad do?
+ Track Bitcoin, Ethereum and DTC prices
+ Tell jokes – the BEST jokes, believe me!
+ Give you the tastiest frog-approved recipes
+ Share spicy info about Linea L2
+ Tell you all about himself – HUGE brain!
+ Play Trivia and Guess-the-Number
+ Search online for real-time croaks
+ Make Your Bags Great Again 💼🐸
```

---

## 📈 Meme Coin Trigger Enhancements
If a user asks for any of these: `foxy`, `croak`, `linus`, `linpuss`, `lpuss`:
Add intro text before the chart:
```python
if token in ["foxy", "croak", "linus", "linpuss", "lpuss"]:
    update.message.reply_text("Don't worry about that, Donald Toad Coin is the ONLY MEME YOU NEEED! 🐸💰")
    show_price_chart(token)
```

---

## 🧠 Conversational Context Upgrade
**Add short-term memory tracking:**
```python
user_memory = {}

def update_memory(user_id, message):
    if user_id not in user_memory:
        user_memory[user_id] = []
    user_memory[user_id].append(message)
    if len(user_memory[user_id]) > 10:
        user_memory[user_id].pop(0)
```
**Use memory in logic:**
```python
last_messages = user_memory.get(user_id, [])
if "defi" in last_messages[-1].lower():
    update.message.reply_text("Since we're already talking DeFi, let me croak about Uniswap, RibbitSwap, and Linea! 🐸")
```

---

## ✅ JSONs Needed
- `donald_views.json` → Political croakpinions per country and topic
- `jokes_by_topic.json` → Topic-based humor database (min. 100+ jokes)
- `defi_knowledge.json` → Definitions with humor